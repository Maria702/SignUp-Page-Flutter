import 'package:flutter/material.dart';

// home according to sir
class Status extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.symmetric(vertical: 10),
      child: Column(
        children: [],
      ),
    );
  }
}
